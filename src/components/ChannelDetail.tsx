import { FunctionComponent } from "react";

interface ChannelDetailProps {}

const ChannelDetail: FunctionComponent<ChannelDetailProps> = () => {
  return <>Channel Detail</>;
};

export default ChannelDetail;
